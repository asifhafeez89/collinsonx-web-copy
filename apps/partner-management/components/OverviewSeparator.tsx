import { Box } from '@collinsonx/design-system/core';

const OverviewSeparator = () => (
  <Box
    sx={{
      height: '100%',
      width: '1px',
      backgroundColor: '#DDDDDD',
    }}
  />
);

export default OverviewSeparator;
